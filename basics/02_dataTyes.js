"use strict"; // all the code will be treated as new version acoording to ECMA
let age // undefined

// null=> standalone value... means its empty no number nothing    .... its type is object
//undefined => means no value is assigned.....  its type is undefined because undefined is its self a datatype
// symbol => for the unquieness 
//bigint => for bigger ints like stocks

// object

